//Problem number 2

#include <bits/stdc++.h>

using namespace std;

int main () {
    unordered_map<char,int> v;

    int c;
    char a;
    int np;
    int nl;

    cin >> c;

    for (int i=0; i < c; i++) {
        cin >> a;

        if (a == '?')
            np++;
        else {
            if (v.find(a) == v.end())
                 
        }
    }
    
}
