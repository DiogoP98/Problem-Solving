#include <iostream>

using namespace std;

int main() {
	int t;
	vector<int> h(n);
	vector<int> c(n);

	for (int i = 0; i < t; i++) {
		int n;

		for(int j = 0; j < n; j++) {
			cin >> h[j];
		}

		for(int j = 0; j < n; j++) {
			cin >> c[j];
		}

		
	}


	return 0;
}